# ---- Python / uv ----
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# ---- dbt ----
transform/target/
transform/dbt_packages/
transform/logs/
transform/.user.yml

# ---- working warehouse: never committed (Decision D-03) ----
# Anchored to the repo root: src/metricmine/warehouse/ is code and IS
# committed (D-11).
/warehouse/
*.duckdb
*.duckdb.wal

# ---- except the demo artifact the hosted app reads (Decision D-03) ----
!demo/demo.duckdb

# ---- raw source downloads: never committed (Decision D-15) ----
data/raw/

# ---- PyAirbyte per-connector venvs, created in the repo root on first run ----
.venv-source-*/

# ---- PyAirbyte record staging, created in the repo root during reads ----
.cache/

# ---- proposer outbox: drafts and records, never committed (Decision D-24) ----
# Approval is the human flow: review, copy into contracts/ on a branch,
# contract-only PR. Rejected drafts never leave the outbox.
# Anchored to the repo root: tests/agents/fixtures/proposals/ holds
# committed golden-profile example proposals (D-25) and IS committed.
/proposals/

# ---- local env / secrets ----
.env
*.local

# ---- Claude Code local overrides: machine-specific, never committed (D-37) ----
.claude/settings.local.json

# ---- OS / editor noise ----
.DS_Store
.idea/
