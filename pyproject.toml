[project]
name = "metricmine"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "airbyte>=0.49.0",
    "dbt-core>=1.11,<1.12",
    "dbt-duckdb>=1.10,<1.11",
    "duckdb>=1.4.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.21",
]
