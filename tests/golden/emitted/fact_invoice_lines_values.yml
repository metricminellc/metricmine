# Generated by metricmine-engine v0.1.0 from gold_invoice_lines_mapping v1.0.0 + gold_unified_event_star v1.0.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: fact_invoice_lines_values
  description: 'The invoice_lines fact: measure payload (quantity, unit_price per mapping v1.0.0), manifest reference, and group-key references. One row per declared transaction grain — one row per silver invoice line (44,721 at silver v1.1.0 over the committed sample; the C1 conservation arithmetic). Composite primary key (fact, source, timeframe, dim) hashes; run_hash_id is deliberately NON-key so a contract version bump never mints duplicate facts (D-17). Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: fact_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Record key over the measure payload (D-18).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_invoice_lines_values__fact_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field fact_hash_id has no missing values
  - name: source_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Group key: resolves in gold.dim_source_values (C2).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_invoice_lines_values__source_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field source_hash_id has no missing values
  - name: timeframe_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Group key: resolves in gold.dim_timeframe_values (C2).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_invoice_lines_values__timeframe_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field timeframe_hash_id has no missing values
  - name: dim_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Group key: resolves in gold.dim_invoice_lines_values (C2).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_invoice_lines_values__dim_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field dim_hash_id has no missing values
  - name: run_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Non-key lineage reference: resolves in gold.dim_run_values (C2) but never participates in the fact grain (D-17).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_invoice_lines_values__run_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field run_hash_id has no missing values
  - name: fact_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key of the measure manifest: resolves in gold.dim_invoice_lines_columns (C2).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_invoice_lines_values__fact_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field fact_col_hash_id has no missing values
  - name: fact_values
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Measure payload: canonical JSON text (C4-gated).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_invoice_lines_values__fact_values__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field fact_values has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_invoice_lines_values__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field loaded_at has no missing values
