# Generated by metricmine-engine v0.5.0 from gold_unified_event_star v1.6.0 over 1 mapping contract (gold_invoice_lines_mapping v1.1.1).
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: dim_timeframe_values
  description: 'Time group, values side: one row per distinct timeframe payload; the conformed calendar (D-17 Amendment R): the declared grain plus each category''s declared time column truncated to that grain, rendered canonically, so equal periods at equal grain are one row whichever category minted them. Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: timeframe_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Record key over the timeframe payload (D-18).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__timeframe_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field timeframe_hash_id has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__timeframe_hash_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field timeframe_hash_id has no duplicate values
  - name: timeframe_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key of the timeframe manifest; resolves in dim_timeframe_columns.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__timeframe_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field timeframe_col_hash_id has no missing values
  - name: timeframe_values
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Timeframe payload: canonical JSON text (C4-gated).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__timeframe_values__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field timeframe_values has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field loaded_at has no missing values
  - name: captured_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: 'Capture watermark carried from silver (D-38): first-seen (minimum) capture timestamp over the silver rows behind this timeframe payload. Outside every hashed payload (rule 13). Required since 1.5.0: the incremental path proved it populated on every row (Arc 5b, D-38), the F-28 follow-up the 1.4.0 declaration promised.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__captured_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field captured_at has no missing values
