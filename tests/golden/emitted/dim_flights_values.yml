# Generated by metricmine-engine v0.5.0 from gold_flights_mapping v1.0.0 + gold_unified_event_star v1.6.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: dim_flights_values
  description: 'flights category group, values side: the dimension-attribute payload per the mapping contract (the dimension-role fields flight_date, carrier_code, flight_number, origin_airport, sched_dep_hhmm, dest_airport, tail_number, is_cancelled, dep_hhmm, arr_hhmm, sched_arr_hhmm, sched_dep_hour_local, sched_dep_minute_local, carrier_name, aircraft_manufacturer, aircraft_model, aircraft_manufacture_year, aircraft_seat_count, aircraft_engine_type, origin_airport_name, origin_municipality, origin_elevation_ft, origin_longest_runway_ft, dest_airport_name, dest_municipality, dest_iso_region, dest_elevation_ft, dest_longest_runway_ft plus the derived flight_identity degenerate identifier), deduplicated by content key, row-unique at transaction grain because flight_identity carries the declared silver grain tuple as one canonical_key v2 digest. Generic column names (dim_*) keep the physical shape source-invariant; the category lives in the table name. Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: dim_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Record key over the dimension payload (D-18).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_flights_values__dim_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field dim_hash_id has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_flights_values__dim_hash_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field dim_hash_id has no duplicate values
  - name: dim_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key of the dimension manifest; resolves in dim_flights_columns. A new mapping dimension mints a NEW schema key here (D-17).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_flights_values__dim_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field dim_col_hash_id has no missing values
  - name: dim_values
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Dimension payload: canonical JSON text (C4-gated).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_flights_values__dim_values__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field dim_values has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_flights_values__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field loaded_at has no missing values
  - name: captured_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: 'Capture watermark carried from silver (D-38): first-seen (minimum) capture timestamp over the silver rows behind this payload. Outside every hashed payload (rule 13). Required from the first version: the model populates it on every row from its first build (F-49).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_flights_values__captured_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field captured_at has no missing values
