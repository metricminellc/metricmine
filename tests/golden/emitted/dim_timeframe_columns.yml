# Generated by metricmine-engine v0.3.0 from gold_invoice_lines_mapping v1.1.0 + gold_unified_event_star v1.2.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: dim_timeframe_columns
  description: 'Time group, columns side: one row per distinct timeframe manifest, keyed by schema key. Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: timeframe_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key over the timeframe manifest in declared order (D-18).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_columns__timeframe_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field timeframe_col_hash_id has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_columns__timeframe_col_hash_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field timeframe_col_hash_id has no duplicate values
  - name: timeframe_columns
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Timeframe manifest: compact JSON array in declared order.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_columns__timeframe_columns__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field timeframe_columns has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_columns__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field loaded_at has no missing values
