# Generated by metricmine-engine v0.1.0 from gold_invoice_lines_mapping v1.0.0 + gold_unified_event_star v1.0.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: dim_invoice_lines_values
  description: 'invoice_lines category group, values side: the dimension-attribute payload per the mapping contract (dimension-role fields plus the derived line_identity degenerate identifier), deduplicated by content key — line-unique at transaction grain because line_identity carries the declared silver grain tuple as one canonical_key v2 digest. Generic column names (dim_*) keep the physical shape source-invariant; the category lives in the table name. Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: dim_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Record key over the dimension payload (D-18).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_invoice_lines_values__dim_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field dim_hash_id has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_invoice_lines_values__dim_hash_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field dim_hash_id has no duplicate values
  - name: dim_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key of the dimension manifest; resolves in dim_invoice_lines_columns. The signature test announces itself here: a new mapping dimension mints a NEW schema key (D-17).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_invoice_lines_values__dim_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field dim_col_hash_id has no missing values
  - name: dim_values
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Dimension payload: canonical JSON text (C4-gated).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_invoice_lines_values__dim_values__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field dim_values has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_invoice_lines_values__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field loaded_at has no missing values
