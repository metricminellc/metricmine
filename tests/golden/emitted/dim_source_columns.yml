# Generated by metricmine-engine v0.2.0 from gold_invoice_lines_mapping v1.0.0 + gold_unified_event_star v1.2.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: dim_source_columns
  description: 'Source-system group, columns side: one row per distinct source manifest (field set), keyed by schema key. Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: source_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key: canonical_key v2 over the manifest in declared order (D-18).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_columns__source_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field source_col_hash_id has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_columns__source_col_hash_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field source_col_hash_id has no duplicate values
  - name: source_columns
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Source manifest: compact JSON array of field names in declared order.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_columns__source_columns__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field source_columns has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_columns__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field loaded_at has no missing values
