# Generated by metricmine-engine v0.4.0 from gold_invoice_lines_mapping v1.1.1 + gold_unified_event_star v1.4.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: dim_source_values
  description: 'Source-system group, values side: one row per distinct source payload, deduplicated by content key. Constant per build per source. Engine-emitted (D-09); hashed payload carries deterministic content only (rule 13).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: source_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Record key: canonical_key v2 over the source payload (D-18); 64-char lowercase SHA-256 hex.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_values__source_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field source_hash_id has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_values__source_hash_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field source_hash_id has no duplicate values
  - name: source_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key of the source manifest; resolves in dim_source_columns.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_values__source_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field source_col_hash_id has no missing values
  - name: source_values
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Source value payload: canonical JSON text (compact, sorted, lowercase); json_valid-gated (C4, F-15).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_values__source_values__field_required
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field source_values has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_source_values__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field loaded_at has no missing values
