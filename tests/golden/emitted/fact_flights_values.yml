# Generated by metricmine-engine v0.5.0 from gold_flights_mapping v1.0.0 + gold_unified_event_star v1.6.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: fact_flights_values
  description: 'The flights fact: measure payload (dep_delay_minutes, arr_delay_minutes, air_time_minutes, distance_miles per the mapping contract), manifest reference, and group-key references. One row per declared transaction grain: one row per silver.silver_flights row (the C1 conservation arithmetic). Composite primary key (fact, source, timeframe, dim) hashes; run_hash_id is deliberately NON-key so a contract version bump never mints duplicate facts (D-17). Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: fact_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Record key over the measure payload (D-18).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__fact_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field fact_hash_id has no missing values
  - name: source_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Group key: resolves in gold.dim_source_values (C2).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__source_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field source_hash_id has no missing values
  - name: timeframe_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Group key: resolves in gold.dim_timeframe_values (C2).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__timeframe_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field timeframe_hash_id has no missing values
  - name: dim_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Group key: resolves in gold.dim_flights_values (C2).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__dim_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field dim_hash_id has no missing values
  - name: run_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Non-key lineage reference: resolves in gold.dim_run_values (C2) but never participates in the fact grain (D-17).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__run_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field run_hash_id has no missing values
  - name: fact_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key of the measure manifest: resolves in gold.dim_flights_columns (C2).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__fact_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field fact_col_hash_id has no missing values
  - name: fact_values
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Measure payload: canonical JSON text (C4-gated).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__fact_values__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field fact_values has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field loaded_at has no missing values
  - name: captured_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: 'Capture watermark carried from silver (D-38): the contributing silver row''s capture timestamp, per fact row. Outside every hashed payload (rule 13). Required from the first version: the model populates it on every row from its first build (F-49).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: fact_flights_values__captured_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.6.0
              generated: true
        description: Check that field captured_at has no missing values
