-- AUTO-GENERATED by `datacontract dbt sync`. Do not edit.
-- Source contract: silver_airport_weather@1.0.0 (model: silver_airport_weather)
{{ config(severity='error', meta={"datacontract_cli": {"check": "silver_airport_weather__custom_sql", "contract_versions": ["1.0.0"], "generated": true, "include_in_tests": true, "model": "silver_airport_weather", "description": "Grain enforcement: no duplicate (airport_code, observed_hour_utc) combinations. Declared at error severity because the primaryKey-flag composite test generates at warn severity at datacontract-cli 1.0.12 (rule 5: uniqueness as a test, never a trusted constraint)."}}) }}
WITH _dc_metric (metric_value) AS (
SELECT COUNT(*) FROM (
  SELECT airport_code, observed_hour_utc
  FROM {{ ref('silver_airport_weather') }}
  GROUP BY airport_code, observed_hour_utc
  HAVING COUNT(*) > 1
)
)
SELECT metric_value FROM _dc_metric WHERE metric_value IS NULL OR metric_value <> 0
