# Generated by metricmine-engine v0.3.0 from gold_invoice_lines_mapping v1.1.1 + gold_unified_event_star v1.3.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: context_registry
  description: 'The context registry (D-19): one row per schema key present in the star, binding the address of meaning to its governing contract and the compiled context gathered at approval time. Rows are SQL VALUES literals carried from the committed compiled-context artifact at emission time (D-30): every warehouse write stays inside dbt build, and no build-time file read ever occurs. Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: schema_key
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key: canonical_key v2 over the governed manifest in declared order (D-18); 64-char lowercase SHA-256 hex.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: context_registry__schema_key__field_required
              include_in_tests: true
              contract_versions:
              - 1.3.0
              generated: true
        description: Check that field schema_key has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: context_registry__schema_key__field_unique
              include_in_tests: true
              contract_versions:
              - 1.3.0
              generated: true
        description: Check that field schema_key has no duplicate values
  - name: entity_group
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Entity group the key belongs to: source, run, timeframe, or the category''s declared entityGroup.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: context_registry__entity_group__field_required
              include_in_tests: true
              contract_versions:
              - 1.3.0
              generated: true
        description: Check that field entity_group has no missing values
  - name: contract_name
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Governing contract id: the mapping contract whose approval created this schema key.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: context_registry__contract_name__field_required
              include_in_tests: true
              contract_versions:
              - 1.3.0
              generated: true
        description: Check that field contract_name has no missing values
  - name: contract_version
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Governing contract version at compile time.
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: context_registry__contract_version__field_required
              include_in_tests: true
              contract_versions:
              - 1.3.0
              generated: true
        description: Check that field contract_version has no missing values
  - name: compiled_context
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Compiled context gathered at approval time: canonical compact JSON text harvested from the governing contracts by the context compiler (D-30). Addressed content, never a hashed payload, so it is NOT lowercased; json_valid-gated below.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: context_registry__compiled_context__field_required
              include_in_tests: true
              contract_versions:
              - 1.3.0
              generated: true
        description: Check that field compiled_context has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: context_registry__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.3.0
              generated: true
        description: Check that field loaded_at has no missing values
