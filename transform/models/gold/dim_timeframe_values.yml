# Generated by metricmine-engine v0.4.0 from gold_invoice_lines_mapping v1.1.1 + gold_unified_event_star v1.4.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: dim_timeframe_values
  description: 'Time group, values side: one row per distinct timeframe payload; the mapping contract''s declared time column truncated to its declared grain (invoiced_at at minute grain for invoice_lines), rendered canonically. Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: timeframe_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Record key over the timeframe payload (D-18).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__timeframe_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field timeframe_hash_id has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__timeframe_hash_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field timeframe_hash_id has no duplicate values
  - name: timeframe_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Schema key of the timeframe manifest; resolves in dim_timeframe_columns.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__timeframe_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field timeframe_col_hash_id has no missing values
  - name: timeframe_values
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Timeframe payload: canonical JSON text (C4-gated).'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__timeframe_values__field_required
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field timeframe_values has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_timeframe_values__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.4.0
              generated: true
        description: Check that field loaded_at has no missing values
  - name: captured_at
    data_type: TIMESTAMP
    description: 'Capture watermark carried from silver (D-38): first-seen (minimum) capture timestamp over the silver rows behind this timeframe payload. Outside every hashed payload (rule 13). Optional under the F-28 rule; the declared follow-up tightens it to required once the incremental path is proven populated.'
