# Generated by metricmine-engine v0.2.0 from gold_invoice_lines_mapping v1.0.0 + gold_unified_event_star v1.2.0.
# Engine-owned (D-09): do not edit; flag drift instead (rule 8). Spec: docs/spec/engine.md.
version: 2
models:
- name: dim_run_values
  description: 'Lineage group, values side: mapping contract name and version plus engine version as deterministic content (D-17) — lineage as data, never a build timestamp. run_hash_id is referenced by the fact as a NON-key attribute, so a contract version bump never mints duplicate facts. Engine-emitted (D-09).'
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: gold_unified_event_star
  columns:
  - name: run_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Record key over the run payload (D-18).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_run_values__run_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field run_hash_id has no missing values
    - unique:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_run_values__run_hash_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field run_hash_id has no duplicate values
  - name: run_col_hash_id
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: Schema key of the run manifest; resolves in dim_run_columns.
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_run_values__run_col_hash_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field run_col_hash_id has no missing values
  - name: run_values
    data_type: VARCHAR
    constraints:
    - type: not_null
    description: 'Run payload: canonical JSON text; deterministic content only (rule 13) — mapping contract name + version, engine version.'
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_run_values__run_values__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field run_values has no missing values
  - name: loaded_at
    data_type: TIMESTAMP
    constraints:
    - type: not_null
    description: Audit stamp; outside every hashed payload (rule 13).
    data_tests:
    - not_null:
        config:
          severity: warn
          meta:
            datacontract_cli:
              check: dim_run_values__loaded_at__field_required
              include_in_tests: true
              contract_versions:
              - 1.2.0
              generated: true
        description: Check that field loaded_at has no missing values
