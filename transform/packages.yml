# Pinned exactly, never latest (rule 1 ethos). Verified current on the dbt
# package hub at drafting time; requires dbt-core >=1.3.0,<3.0.0, compatible
# with the 1.11.x pin. package-lock.yml is committed; dbt_packages/ is not.
packages:
  - package: dbt-labs/dbt_utils
    version: 1.3.3
