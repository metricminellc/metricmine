# Arc 5b exit record: Record 008 part two, the C5 bump, and the scale posture

> Repo path: `docs/verification/evidence/2026-09-01_arc5b_exit.md`
> The receipt for Arc 5b, one of Recommendation Rev 7's seven v1.0.0
> conditions (the Arc 5 condition, opened by Arc 5a and completed here).
> Closes #93.

Arc 5b landed as nine pull requests in ladder order, every one squash
merged with green checks and verified against the prep bundle.

| PR | Squash on main | What landed |
|---|---|---|
| #130 | a70e8cb36c4a0af3156164b9406cb4a3597583d0 | Decision Record 008 part two: D-38 (incremental materialization behind engine.materialization), D-39 (batch-scoped gates; make audit-gold), D-40 (docs/scale.md and the measurement rule); Amendments O (D-07), P (D-17), Q (D-36); findings F-34 and F-35; the spec and CLAUDE.md edit set; docs/scale.md. |
| #131 | 5eaba14701402f8b98db99f1e16e85f4d0c92e8a | The diagrams payload: the README layer-flow overview pair and the runtime workflow refreshed to the brand standard, light and dark with Mermaid twins and 2x PNG exports. |
| #132 | 697b8d766148c525315d6eb2d0f0d6dec761ee2f | The README wiring: the overview embed, click-through anchors on both raw-HTML picture blocks, the docs index rows, and the two superseded files removed. |
| #133 | 04619834af6282f26a2f97ef39c6c0c7877c71f1 | The gold star contract 1.3.0 (C5, the field-level reconciliation, promoted per V1-06) and the mapping contract 1.1.1; compiled context v0005; the byte oracle refreshed beside its contracts; the F-19-class version literal left test_render.py. |
| #134 | 0110a6a79c48f3f0b4d6ab910be8ae676c112fab | The 1.3.0 regeneration as its own reviewed pull request: 25 emitted files, the 35 sync-generated singular tests including C5, and the demo artifact refresh (D-33). |
| #135 | 6ad6e8664c7ae10d6bf420479286ce3df4a121ac | The incremental-readiness contracts: star 1.4.0 (captured_at optional-first under F-28; the mm_batch_floor guards) and silver 1.2.0 (captured_at carried from bronze); compiled context v0006; the oracle refreshed. |
| #138 | 90af34c92fa2a893ef8defef4720038a5771fdc8 | The implementation: engine.materialization with fail-closed validation, the captured_at plumbing, the is_incremental blocks inert under table, on_schema_change fail (F-34), make audit-gold, ENGINE_VERSION 0.4.0, the regeneration, the oracle, and the demo refresh. |
| #139 | f6d2f51e67f7093f2ff154ed281dcfd71a1daea8 | docs/scale.md gains the Mac re-measure with its environment stated (D-40). |
| #140 | the squash of this pull request | This exit record. |

## The gates, measured at the close

The pytest lane grew 466 to 468 (the two D-38 mode tests); the hooks
lane held at 55; gate 2 grew 96 to 97 data tests and 109 to 110 passes
(C5 is the one new gate); make audit-gold runs the 35 contract-generated
gold tests in their unscoped form; the demo content digest held at
08eca7be30707e2aa0b48c3d19ddeea4 through every landing (the typed view
never carries run lineage or audit columns, so the re-keys and the new
captured_at column leave it untouched); regeneration stayed idempotent
and byte-stable at every step.

## The incremental proof

On the implementation branch, never committed: the mode flip regenerated
as exactly one config line per model plus the manifest; an incremental
no-op build preserved every count; two synthetic batches (100 rows, then
50) grew silver, the fact, the category dimension, and the mart by
exactly their sizes with the timeframe dimension deduplicating and the
run and source dimensions holding at one row; the floor-scoped gate run
passed with the bound predicate confirmed in the compiled SQL; the
full-table audit passed; and a table-mode rebuild restored the baseline
byte-clean.

## Em dashes that left, and what remains by design

At the bump and the regeneration: the contracts' twelve, the golden
fixtures' ten, and the emitted properties files' six; at the emitter
touch, the engine package's six comment em dashes; at the diagram
refresh, the runtime workflow twin's three. Remaining by design: the two
quoted Cannot sync tool messages, the frozen fixture, the test literal,
the evidence files, and the two earlier diagrams with their twins
(agent proposal flow, gold star flow), which re-render at Arc 4.

## Deferred and carried forward

The Mac re-measure numbers live in docs/scale.md with their environment.
X-03 (the case-preserving payload) stays post-tag. The F-28 follow-up
that tightens captured_at to required lands at a later register touch
once the incremental path has run in anger. The earlier diagrams'
re-render, the issue templates, a DCO check, and the contracts/
provenance CI test stay Arc 4 candidates.

Closes #93.
