# Arc 6 exit record: the multi-source proof

> Repo path: `docs/verification/evidence/2026-09-05_arc6_exit.md`
> The receipt for Arc 6, the first arc of the stable line (Recommendation
> Rev 8; Decision Record 011, D-41). Closes #154.

Arc 6 landed as twenty-three pull requests in ladder order, every one squash
merged with green checks and verified against the prep bundle before the
merge, with eight deviations recorded in the execution record and none of them in a merged tree: (1) runbook prose on where staged deletions ride and on one status count; (2) at PR 2 the asset download failed the TLS handshake on a python.org CPython 3.12 with no CA bundle wired into OpenSSL, and the merge ran before that condition had printed, so the download was measured afterward on both machines with `SSL_CERT_FILE` set from the lock's certifi (the fallback is carried forward below); (3) at PR 5 the outbox path is per agent and the direction line prints the D-35 classes, where the runbook named an older path; (4) the structural byte gate imports PyYAML, so it ran under `uv run python`; (5) at PR 6 the sync had written the live draft's wording into the properties file the edit set assumed carried the reference wording, so the constraint was applied by a description-independent edit and that one file compared without prose; (6) at PR 17 the restore line globbed untracked files and restored nothing, with nothing to restore; (7) the 1.1.0 changelog heading landed with its day unfilled, and the sitting that tagged filled it in #176 as a second commit; (8) the first fill of this record refused three blank values (the deviations, the live calls, and their cost, which only the sittings could know) and ran again with them supplied. The tag `v1.1.0` sits on the squash of
#176 (`2055b4cd6eb125f8c88733571220b88d1c0ac4c1`); the release carries the demo artifact a cold
build of that commit produced as its one asset, and the manifest this
pull request lands names it.

| PR | Squash on main | What landed |
|---|---|---|
| #155 | 10bb970b098f97848736e3d9696930d668da65a7 | Decision Record 011: D-41 with its claim and its trial, Amendments R through W, findings F-36 through F-53, the specs and CLAUDE.md transcribed, the changelog entry. |
| #156 | eeff0e777e8e36e5e0b1c136cf09157e188dd9d2 | Gold star 1.5.0, contract-only: the conformed calendar, `captured_at` required on the silver-derived objects, `tail_number` among the conformed key rules; compiled context v0007 and the oracle. |
| #157 | eb3c62abbc3c6da61d6e68131e81c2589a55e51e | The demo artifact as a release asset under a committed digest manifest (Amendment S): `make demo-fetch`, the doctor line, the CI gate, the artifact out of the index. |
| #158 | 4b7508cf7c02a3c38ae617baf6ca5252e8842024 | The engine fan-in over `engine.mapping_contracts` (Amendment U), the conformed calendar in the emission (R), compiled schema 2.0.0 (V), engine 0.5.0, the regeneration over one category, the 1.5.0 tests. |
| #159 | 960e401c60eda5d027cd1bc0cb4eec5a4ce0861a | The landing loop over `ingestion.sources` and the profiler's `--only` selector. |
| #160 | d6cb4a2cd9c9f56512af9998a443edbe86b1f755 | `silver_invoice_lines` 1.3.0 through the amend stance: `captured_at` required (the F-28 follow-up, F-46); its profile v0002 and compiled context v0009. |
| #161 | 6c6317c7820ea1c188fb3b12af404d96387cbcba | The model-plane refresh for silver 1.3.0: the properties file, the generated tests, the ownership manifest. |
| #162 | df3a76d1f15c7b6b90394551921365ae04d16a11 | OurAirports airports and runways at commit `d27027ba`, `fetch_common`, and the committed-samples gate (Amendment T). |
| #163 | 91de81006cc9eb7c7b4a53576ea06a3fb3cfdbe2 | The nycflights13 family at commit `df98ef21`: flights (January through June 2013), weather, airlines, planes, each with its README and its digest-checked fetch script. |
| #164 | 64bf238b09d070d27052344d2eacab7babdc22c0 | The six aviation extracts landed through the one file connector with their reader options (F-50) and their bronze profiles. |
| #165 | a25f229f983a64401501f1b9cb674f1176378211 | Six cleanup contracts hand-authored from their profiles, the conformed keys declared, and the K1 gate (`tests/test_conformed_keys.py`). |
| #166 | 890d2cc9ea47d7050c075ae1b38b768099175969 | The six cleanup models, their properties, their generated tests, and their silver profiles; the first cold build of the family (PASS=204). |
| #167 | ffa5dc147c72c9fc87baa5f795504e5bd63e43b9 | `silver_flights` and `silver_airport_weather` 1.0.0 with their joins declared and measured, and the profiles they cite. |
| #168 | 17044196258518f3285da5a2b88cecef0ddaf6da | The two unified tables in human-owned SQL with their properties and their generated tests, the rules naming the table through `ref()` (F-51); PASS=254. |
| #169 | 684be6c31f2423044dec6629b127edb42b85d3bc | Amendment W in code: `data` and `expert_context` apart on every registry entry, the subject and the context keys on the listing, the server instructions; compiled context v0010. |
| #170 | 0e0c15d89477133a9406ad93c8d41aa794d3cd3b | The `flights` and `airport_weather` mapping contracts. |
| #171 | 1acc5ed75eabda546cfaa4cf0ec9f9689afdf0b7 | Gold star 1.6.0: the two categories' fifteen objects, `crossCategoryJoins` with the measured completeness and the worked example, the declared-join gate (`tests/test_declared_joins.py`); compiled context v0011. |
| #172 | 8ba578d2e5ddefdc7c7994eb77b4b8e152b305bf | The regeneration over three categories (engine 0.5.0, 45 emitted files), compiled context v0012, the 1.6.0 tests, the manifest over the split registry, and the aviation conservation gate; PASS=334, audit 67. |
| #173 | 0337e384b0e8a8aa8b6d1eaf3b8590030d6a42f0 | The family selectors (`SOURCE=`, `TABLE=`, `TARGET=`, `ORACLE=`), the mapping agreement scorer, and the eight oracle fixtures for the eval lane. |
| #174 | 10a078348b306db04e22ed5db5a85f06181a4467 | The demo question set (Q1 to Q7) with its answers measured at the committed samples, proven through the serving path (D-41 M3). |
| #175 | 9a5655308f0710c04efc9d8eafebe3979e81836c | The README, the demo guide, the sources register, the adding-a-source walkthrough (validated by a source that is not in the demo), the demo sources explained, the operator's manual, the scale curve, and the 1.1.0 changelog cut. |
| #176 | 2055b4cd6eb125f8c88733571220b88d1c0ac4c1 | The D-41 verdict paragraph in the register: the trial passes. |
| #177 | the squash of this pull request | The manifest naming v1.1.0, the front door naming the release, and this exit record. |

## The trial (D-41), judged at the close

| Criterion | Measured | Held |
|---|---|---|
| M1 build: every category in one `dbt build`, C1 to C5 at error | gate 2 `Found 31 models, 303 data tests, 7 sources, 617 macros`, `PASS=334`; `make audit-gold` `PASS=67`; `make demo` keyless; the manifest gate `demo manifest matches the built warehouse content: PASS` | yes |
| M2 conformance: the K1 gate and the declared join floors | `tests/test_conformed_keys.py` 3 passed; conformed keys `airport_iata, carrier_code, tail_number`; join completeness carrier 1.0000 (floor 1.00), aircraft 0.8396 (0.80), origin 1.0000 (1.00), destination 0.9791 (0.97), airport 1.0000 (1.00); the cross-category join flights_to_origin_weather 0.9994 (0.99) through the typed surfaces and through silver alike (`tests/test_declared_joins.py`) | yes |
| M3 service: Q1 to Q7 through the typed surfaces, one `query` each | `tests/test_serving_questions.py` 9 passed: Q1 to Q7 answered on the typed surfaces as recorded in `tests/fixtures/serving_questions.json`, (Q2, Q3, Q7 across categories on the declared join; Q1, Q4, and Q6 on joins settled in silver, Q4 the vintage effect; Q5 the retail control), `list_fact_categories` and `get_context` the only guide | yes |

Verdict: the trial passes. The measured cost of per-category dimensions
(F-44): the flights category's values dimension is 59 MB of a 106 MB artifact (flights 88 MB in all: dimension 59, fact 16, mart 13), the price of a wide payload chosen so the whole unified row reaches the typed surface. The verdict paragraph under D-41 landed in the
register by #176, written against the criterion Record 011
fixed before the first source landed.

## The agreement study (D-25)

The eval lane ran once after PR 18 (`make eval-agents`, claude-sonnet-5,
eleven fixtures: the three that existed before the arc, without an
oracle, and the eight family fixtures bound to their human-authored
contracts as oracles). Every draft was written at the first attempt;
first-attempt lint 11/11; first-attempt groundedness 11/11; tokens in
89,433, out 76,225; cost $0.9411 (the per-row costs below are rounded).
Agreement is the scorer's count of agreeing first-class fields over the
fields checked on the column names both sides share; a column on one
side only is a mismatch; the three pre-arc fixtures have no oracle and
are not scored.

| Fixture | Proposer, stance | Oracle | Agreement | Mismatches | Cost |
|---|---|---|---|---|---|
| silver-cleanup-online-retail | silver-cleanup-proposer, cleanup | none | - | - | $0.0978 |
| mapping-propose-invoice-lines | gold-mapping-proposer, propose | none | - | - | $0.0609 |
| silver-cleanup-messy-orders | silver-cleanup-proposer, cleanup | none | - | - | $0.1259 |
| silver-cleanup-nyc-flights | silver-cleanup-proposer, cleanup | `contracts/silver_nyc_flights.odcs.yaml` | 0/0 | 38 | $0.0979 |
| silver-cleanup-nyc-weather | silver-cleanup-proposer, cleanup | `contracts/silver_nyc_weather.odcs.yaml` | 0/0 | 29 | $0.0835 |
| silver-cleanup-nyc-airlines | silver-cleanup-proposer, cleanup | `contracts/silver_nyc_airlines.odcs.yaml` | 0/0 | 5 | $0.0363 |
| silver-cleanup-nyc-planes | silver-cleanup-proposer, cleanup | `contracts/silver_nyc_planes.odcs.yaml` | 10/10 | 15 | $0.0475 |
| silver-cleanup-ourairports-airports | silver-cleanup-proposer, cleanup | `contracts/silver_ourairports_airports.odcs.yaml` | 61/65 | 15 | $0.1054 |
| silver-cleanup-ourairports-runways | silver-cleanup-proposer, cleanup | `contracts/silver_ourairports_runways.odcs.yaml` | 30/30 | 18 | $0.0869 |
| mapping-propose-flights | gold-mapping-proposer, propose | `contracts/gold_flights_mapping.odcs.yaml` | 131/132 | 3 | $0.1288 |
| mapping-propose-airport-weather | gold-mapping-proposer, propose | `contracts/gold_airport_weather_mapping.odcs.yaml` | 67/68 | 3 | $0.0701 |

The mapping proposer nearly reproduces the human mapping contracts from
the silver contract it reads. The cleanup proposer, working from the
bronze profile alone, keeps the source's column names where the human
contracts renamed them with business meaning, so three fixtures share no
column name with their oracle. The business context lives in the names
and the rules; that is why the family's contracts are human-authored,
and one run per fixture is a reading, not a rate. The eight
`proposal.json` files stay in the study's outbox until the follow-up
pull request records them.

## The sources, as landed

| Extract | Rows | Bytes | Extract sha256 | Bronze table |
|---|---|---|---|---|
| `online_retail_ii_2009-12.csv` | 45,228 | 3,958,357 | `81cde97033ae028f4a7e6b1a9c838792b3f6ac5f0a186b5fd06720d733a71a1c` | `bronze.online_retail_ii` |
| `nyc_flights_2013-01_2013-06.csv` | 166,158 | 15,215,202 | `ea3bce0babe2b330db58b7b16ef8a2c1fa346d71c7f37da2ead495260b7bef18` | `bronze.nyc_flights` |
| `nyc_weather_2013-01_2013-06.csv` | 13,014 | 1,149,905 | `4092d00f522d3860122c0e41b3cfa96931e660366273f13567b84c9a7342fe77` | `bronze.nyc_weather` |
| `nyc_airlines_df98ef215aa8.csv` | 16 | 386 | `162551bd3401a12d63db3d92b7e66af3017d2e40d55919d6a678489323c10609` | `bronze.nyc_airlines` |
| `nyc_planes_df98ef215aa8.csv` | 3,322 | 247,198 | `778962edec8339f6f6edb1d6506869f61cab573eda03d7e162d2899c76d04c1a` | `bronze.nyc_planes` |
| `ourairports_airports_d27027ba4414.csv` | 9,057 | 1,664,615 | `34bbc6719ee42e42be6f5fb4480129c8ab33951005ff8995e14f604df98b3f91` | `bronze.ourairports_airports` |
| `ourairports_runways_d27027ba4414.csv` | 10,760 | 1,144,263 | `de59705512170f8733ee30caaab4833912a7bb8319604eb12e75cde519dfd52b` | `bronze.ourairports_runways` |

Each extract regenerated byte-identically on this machine from the
pinned raw bytes (the D-15 two-machine proof; the sandbox was the
first machine).

## The gates, measured at the close

The pytest lane grew 483 to 548 (with 18 skipped by name until the eight recordings land); the local lane 76
of 76; the hooks lane held at 55; ruff clean; gate 3 sync
updated 0 YAML files over 13 contracts; the registry
digest `d431a853c6b499c86a00c6cdf3113de7`; `make doctor` reported `doctor: 7 checks, 0 FAIL, 0 WARN`. Live
proposer calls: twelve (one at PR 5 through the amend stance; eleven in the eval study after PR 18, every draft written at the first attempt) at claude-sonnet-5, $1.03 ($0.0879 at PR 5 and $0.9411 for the study) in total
(ruling 8's bound $2.50). Every fetched ref verified against the prep
bundle by the byte gate or the structural gate the bundle names for it.

## Deferred and carried forward

- The eight recorded renders for the family fixtures: their tests skip by
  name until the live eval study's `proposal.json` files land under
  `tests/agents/fixtures/recorded/` in a follow-up pull request cut from
  the study's report.
- The full 2013 year for the nycflights13 extracts stays a fetch-script
  parameter; the committed window is January through June (Amendment T's
  20 MB event budget).
- Shared business-entity dimensions stay out of gold by the verdict; a
  failing verdict would have made them the next register amendment.
- dbt 1.12 stays the line (D-05 Amendment N); dbt Core v2 stays deferred.
- A certifi fallback in `scripts/fetch_demo.py` and `scripts/fetch_common.py`,
  with a line in `docs/demo.md`, for Python builds that ship no CA bundle
  wired into OpenSSL; this arc measured the workaround (`SSL_CERT_FILE`
  from the lock's certifi) on one such build.

Closes #154.
