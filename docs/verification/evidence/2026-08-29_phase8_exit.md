Phase 8 exit record, August 29, 2026: agent ops, the Stage 6 practicum

Repo head at the start of the phase: e996672ce36b73a9739345f93429764118bfd07e (main, the #105 squash). Spine issue: #106. One sitting on August 29, 2026, the designed split point passed through.

What landed, in order, each by a reviewed pull request and a squash merge onto main:
1. #107 (b881ae98d30202f6ff45d7d6386425fec557515e): D-37, local enforcement hooks in the SDLC layer, and F-32, with the prep transcript as evidence (Decision Record 010).
2. #108 (ce881a224da3c880bb9507f2a1ae62ae4a68fac1): the working-tree guard, a PreToolUse hook committed at .claude/settings.json with its script under .claude/hooks/ and 40 subprocess tests in the CI lane; demonstrated live in Claude Code on this Mac, where the guard denied `find ~ -maxdepth 1 -name '*.txt'` with the reason `working-tree guard: Bash names <home>, which resolves outside the project root <repo>. CLAUDE.md Conventions: never read or write paths outside the repository working tree. Stage the input inside the repository, or use a path under it, and retry.` (<home> and <repo> stand for the Mac's home directory and the clone, the evidence-file convention) and passed the in-tree control.
3. #109 (9f0b67f74feac9f8545e242ed8a87f2f64c77709): the contract-review Skill; its paths scope was dropped after measurement (a path-scoped project skill is not invocable by name in a fresh session on Claude Code 2.1.251); its first run over contracts/silver_invoice_lines.odcs.yaml returned `READY (every item yes)`.
4. #110 (73fb434dd2037e68fb38b1232231bae17f42e3d3): the guard refined on three false positives measured at the sitting: a slash-opening Bash token whose first segment names nothing on disk is prose (a slash command in a commit body, a sed expression); plan files move inside the tree (plansDirectory .claude/plans, gitignored); the documented auto-memory directory is allowed for the file tools only. 51 subprocess tests.
5. #111 (90778c59baf02a20e6032a8f948d80f6458e53db): the read-only contract-reviewer subagent preloading the Skill; its first run returned READY on the committed v1.1.1 with one low finding (the silver model's header comment cites v1.1.0; a transform-side chore).
6. #112 (437d5bc7ecf8194330b0563f060d3d8b99ddb23a): the Claude Code GitHub Action workflow (.github/workflows/claude.yml, anthropics/claude-code-action@v1, the subscription token as the repository secret CLAUDE_CODE_OAUTH_TOKEN, --model claude-sonnet-5, --max-turns 50, a 20-minute job timeout, Bash limited to the project's own verification commands). contract-gate is unchanged and stays keyless.
7. #115 (15f2c6972e53da32fa40dfc730e7d87c6bc081c9): the guard allows the runner's own directories on a GitHub Actions runner, measured on the first action run (33253883455), which applied issue #74's edits and committed but could not reach the action's push helper. 55 subprocess tests.
8. #116 (2d37f191e993cb27cef93d7bb0a84baf6c4aa7fb), from action run 33254732917 on issue #74: the residual documentation index drift (five edits to docs/README.md), byte-equal to the prep's expected file. Closed #74.

The receipts per action run: the issue, the triggering comment, the workflow run, Claude's comment with the pull request link, the pull request a person opened, contract-gate green, the review, and the squash merge; every diff was compared byte for byte against the prep's expected file before the merge (no variants).

Measured: 2 action runs in total inside the bound of six (run 33253883455 applied the edits and could not push, the finding behind #115; run 33254732917 (74): 1 min 28 s, success); no credential printed anywhere; make demo, the pytest lane, and contract-gate never referenced the secret. The first action run also measured that the action's own allowed tools merge with --allowedTools from claude_args, and that a comment without the trigger phrase produces a skipped run, not a run.

Deferred by ruling at the sitting: #113 and #114 stay open with their paste-exact bodies and their expected files in the prep record; they run in a later action sitting under the same bound discipline, and their receipts are logged in PROJECT_STATE and closed by their own pull requests.

Stage 6 exit: the two Skilljar courses (Introduction to agent skills, Introduction to subagents) are tracked in the training log; the practicum receipts are the ones above; at least one merged action pull request (above).
