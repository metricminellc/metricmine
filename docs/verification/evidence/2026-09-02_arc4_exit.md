# Arc 4 exit record: the stable-release surface

> Repo path: `docs/verification/evidence/2026-09-02_arc4_exit.md`
> The receipt for Arc 4, one of Recommendation Rev 7's seven v1.0.0
> conditions (the stable release). Closes #141.

Arc 4 landed as eleven pull requests in ladder order, every one squash
merged with green checks and verified against the prep bundle, the
eleventh a fix found at the execution and landed before the tag.

| PR | Squash on main | What landed |
|---|---|---|
| #142 | f32cedc0bef0066dd9246679ddabd624f04ddc52 | The mcp lock refreshed in range to 1.29.1; the pin holds at `>=1.28,<2`. |
| #143 | 2ffaa91509caddfe59b22c27e2e598c014a12e06 | The agent proposal flow and gold star flow published pairs: light and dark, Mermaid twins, 2x PNGs, content current to the register, the 22 em dashes gone. |
| #144 | 331212530570daa642bb700bfd1cd442727c6e1d | The wiring: index rows to pair form, both specs embedding their pairs at block level with click-through anchors, the two superseded files gone. |
| #145 | c7f180e019dcf2df99d3b33cf458f15c3a44269a | Issue forms for bugs, findings, and contract changes, plus the chooser with the security link. |
| #146 | 122a182e9571ae34aca5a8575088c1b12e23ad40 | The DCO check, blocking from its first pull request: pull request commits only, automation exempt by listed identity; CONTRIBUTING and CLAUDE.md state it. |
| #147 | 0dc78c7974030df9210b092a2a75eaa8edbe4369 | The contracts provenance gate: rule 16 and engine spec section 9 as a CI test with discriminating negative fixtures. |
| #148 | cb79040710bd3ea2d094e42288d89b2b2a216e7b | `make doctor` and the demo-artifact content gate, wired into contract-gate so CI proves the stranger path end to end. |
| #149 | 54faea01681ddf309e97982f88a777f9952c378a | The devcontainer: the pinned Python 3.12 image, `uv sync --frozen` at create, doctor as the first run, hermetic ingest. |
| #150 | 4b84aa762c5aad98aa5afcae6d242bb0ebcd9003 | The changelog catch-up ([0.3.0] backfilled, Unreleased reset) and the README release surface (v0.3.0 status, scale pointers, the support matrix). |
| #151 | 9eabe87147be80ad5f561a06f5af596d3fe43ccb | The doctor uv line (finding E-1 of the execution record): `make doctor` reports the uv that manages the checkout, not the venv's locked `uv` package; the changelog's Fixed entry rides with it. |
| #152 | the squash of this pull request | This exit record. |

## The gates, measured at the close

The pytest lane grew 468 to 483 (the provenance
gate's tests); the hooks lane held at 55; ruff clean; gate 2
held at `13 models, 97 data tests, 1 source, 617 macros` with `PASS=110`; `make audit-gold`
PASS=35; the demo-artifact gate reported
`demo artifact matches the built warehouse content: PASS` with the view
digest `08eca7be30707e2aa0b48c3d19ddeea4`; `make doctor` reported
`doctor: 7 checks, 0 FAIL, 0 WARN`; the DCO check ran green on every pull request from
#146 onward. Every fetched ref verified byte-equal against the prep
bundle: the payload rungs through the bundle's check script on the Mac,
the edit rungs by independent application of the prompts in a Linux
sandbox; #143 was verified after its merge (deviation 3 of the execution
record), the rest before.

## The devcontainer receipt

Pending at this record: the first live Codespace run is the async receipt after the tag; if the bootstrap fails, the fix lands as its own small pull request and the receipt follows it.

## Em dashes that left, and what remains by design

The two re-rendered diagrams and their twins carried the last 22; they
left with the payload. Remaining by design: the two quoted `Cannot sync`
tool messages, the frozen fixture, the test literal, and the evidence
files.

## Deferred and carried forward

The v1.0.0 tag follows this record. The multi-source proof (Arc 6) is
the first arc after the tag; the demo tape and the website refresh
follow it, on the promotion path, so the recording shows the case the
proof makes. X-03 stays post-tag. The F-28 tightening of
`captured_at` to required still waits for the incremental path to run in
anger. The datacontract-cli 1.1.2 evaluation stays post-tag. The runtime
workflow pair's content refresh (C1 to C5 and the shelved hosted-demo
node) is a recorded candidate, not scheduled.

Closes #141.
